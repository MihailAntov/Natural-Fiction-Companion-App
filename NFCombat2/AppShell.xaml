<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="NFCombat2.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:NFCombat2"
    Shell.FlyoutBehavior="Disabled">

    <!--<ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />-->

    <TabBar>
        <Tab Title="Episode" Icon="character.png">
            <ShellContent ContentTemplate="{DataTemplate local:FightPage}" />
        </Tab>
        <Tab Title="Fight" Icon="fight.png">
            <ShellContent ContentTemplate="{DataTemplate local:FightPage}" />
        </Tab>

        <Tab Title="Fight" Icon="inventory.png">
            <ShellContent ContentTemplate="{DataTemplate local:FightPage}" />
        </Tab>

        <Tab Title="Fight" Icon="dice.png">
            <ShellContent ContentTemplate="{DataTemplate local:FightPage}" />
        </Tab>

    </TabBar>


</Shell>
