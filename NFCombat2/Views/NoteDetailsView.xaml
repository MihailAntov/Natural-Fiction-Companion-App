<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NFCombat2.Views.NoteDetailsView">
    <VerticalStackLayout>
        <Editor VerticalTextAlignment="Start" Text="{Binding Text}" Placeholder="{Binding EditorPlaceholder}">
            
        </Editor>
        <ImageButton Source="trash" WidthRequest="35" HeightRequest="35">
            
        </ImageButton>
    </VerticalStackLayout>
</ContentView>
