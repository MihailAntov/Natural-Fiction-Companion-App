<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NFCombat2.Views.OptionPickerView">
    <!--<VerticalStackLayout BindableLayout.ItemsSource="{Binding Categories}">
        <BindableLayout.ItemTemplate>
            <DataTemplate>
                
                    <Button Margin="5"
                            HorizontalOptions="StartAndExpand"
                            WidthRequest="120"
                            Text="{Binding}"
                            Clicked="OptionTapped"
                            
                            
                            
                       
                    />
                

            </DataTemplate>
        </BindableLayout.ItemTemplate>

    </VerticalStackLayout>-->

    <ListView ItemsSource="{Binding Categories}" ItemTapped="OptionClicked" IsVisible="{Binding ChoosingCategory}">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <Label
                Text="{Binding}"
                    />
                </ViewCell>

            </DataTemplate>
        </ListView.ItemTemplate>

    </ListView>

    







</ContentView>
